from tkinter import *

caixa = Tk()
caixa.title("Calculadora")
caixa.geometry("300x400")

nome_label=Label(text="", bg="blue", width=300, height=2)
nome_label.place(x=0, y=0)

# botao = Button(caixa, text="", bg="blue")

caixa.mainloop()
